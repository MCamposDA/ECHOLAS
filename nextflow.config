process {
    conda = true
    executor = 'local'

    cpus = 2
    memory = '1.5 GB'
    time = '12h'
}

executor {
    queueSize = 16
}

profiles {
    standard {
        process.maxForks = 8
    }
}